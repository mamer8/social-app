import 'package:flutter/material.dart';
import 'app_local.dart';

String sAppName(BuildContext context) {
  return getLang(context, 'appName');
}

String sHomePage(BuildContext context) {
  return getLang(context, 'homePage');
}

String sUserPage(BuildContext context) {
  return getLang(context, 'userPage');
}

String sChatPage(BuildContext context) {
  return getLang(context, 'chatPage');
}

String sNotificationsPage(BuildContext context) {
  return getLang(context, 'notificationPage');
}

String sPostPage(BuildContext context) {
  return getLang(context, 'postPage');
}

String sLanguageMessage(BuildContext context) {
  return getLang(context, 'changeLanguage');
}

String sLanguageChange(BuildContext context) {
  return getLang(context, 'changeLanguageTo');
}

String sLogOut(BuildContext context) {
  return getLang(context, 'logOut');
}

String sLogoutMessage(BuildContext context) {
  return getLang(context, 'logoutMessage');
}

String sLogin(BuildContext context) {
  return getLang(context, 'login');
}

String sRegister(BuildContext context) {
  return getLang(context, 'register');
}

String sRegisterNow(BuildContext context) {
  return getLang(context, 'registerNow');
}

String sLoginNow(BuildContext context) {
  return getLang(context, 'loginNow');
}

String sHaveAccount(BuildContext context) {
  return getLang(context, 'haveAccount');
}

String sDoNotHaveAccount(BuildContext context) {
  return getLang(context, 'notHaveAccount');
}

String sName(BuildContext context) {
  return getLang(context, 'name');
}

String sEmail(BuildContext context) {
  return getLang(context, 'email');
}

String sPhone(BuildContext context) {
  return getLang(context, 'phone');
}

String sPassword(BuildContext context) {
  return getLang(context, 'password');
}

String sBio(BuildContext context) {
  return getLang(context, 'bio');
}

String sShowMore(BuildContext context) {
  return getLang(context, 'showMore');
}

String sShowLess(BuildContext context) {
  return getLang(context, 'showLess');
}

String sWhatInMind(BuildContext context) {
  return getLang(context, 'whatIsInMind');
}

String sWriteComment(BuildContext context) {
  return getLang(context, 'writeComment');
}

String sLike(BuildContext context) {
  return getLang(context, 'like');
}

String sComment(BuildContext context) {
  return getLang(context, 'comment');
}

String sShare(BuildContext context) {
  return getLang(context, 'share');
}

String sLikes(BuildContext context) {
  return getLang(context, 'likes');
}

String sComments(BuildContext context) {
  return getLang(context, 'comments');
}

String sPosts(BuildContext context) {
  return getLang(context, 'posts');
}

String sPhotos(BuildContext context) {
  return getLang(context, 'photos');
}

String sFollowers(BuildContext context) {
  return getLang(context, 'followers');
}

String sFollowing(BuildContext context) {
  return getLang(context, 'following');
}

String sAddPhoto(BuildContext context) {
  return getLang(context, 'addPhoto');
}

String sAddPost(BuildContext context) {
  return getLang(context, 'addPost');
}

String sPost(BuildContext context) {
  return getLang(context, 'post');
}

String sEditProfile(BuildContext context) {
  return getLang(context, 'editProfile');
}

String sSave(BuildContext context) {
  return getLang(context, 'save');
}

String sTypeMessage(BuildContext context) {
  return getLang(context, 'typeMessage');
}

String sMessageAppearance(BuildContext context) {
  return getLang(context, 'messageAppearance');
}
